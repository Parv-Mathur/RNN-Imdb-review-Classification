services:
  - type: web
    name: imdb-rnn-streamlit
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "streamlit run main.py --server.port=10000"
    pythonVersion: 3.11.0
    plan: free
